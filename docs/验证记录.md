# 3.5.0 验证记录

- 84 项后端 / API 回归通过。新增版本检查、不降级、下载失败、大小与 SHA256 校验、解压安全、异步与互斥、设置持久化、鉴权、重启参数及软件目录内渲染数据保护。
- 独立 Windows 程序以网页服务和原生桌面两种模式完成目录替换；故意使用会退出的测试可执行文件模拟新版本启动失败，成功恢复旧程序并重新启动。中文 / 空格路径、自定义数据目录和服务参数保留。
- 上述替换与回退期间，隔离测试用真实 Blender 保持原 PID 持续产出 PNG；管理器重启后仍识别同一任务 ID 和运行进程，访问密钥与数据标记保持不变。
- 原生 pywebview EXE 就绪、完成队列清理、原图片保留通过。浏览器实测新版本提示、下载进度、校验完成安装入口、自动检查开关、发布说明纯文本展示通过。
- 只读请求 GitHub 正式接口成功；最新 v3.3.1 不会使本地 v3.5.0 降级。更新下载 / UI 使用受控发布响应；没有上传虚构版本，没有操作用户真实渲染。
- 自动更新下载整包；启动回退不是数据库迁移回滚。详见 [v3.5.0 限制](releases/v3.5.0.md)。

## 3.4.0 验证记录

- 70 项后端/API 测试通过。新增完整分批计划推导、当前命令归属校验、脚本修改拒绝、顺序等待验证、未知分支和并行计划拒绝、部分帧退出及非零退出码判定。
- 只读识别用户正在使用的外层脚本，得到 20 批、1–1200 帧。未接管、暂停或修改用户的真实计划及输出。
- 真实 Blender 测试在独立临时目录进行：自动识别 3 批 12 帧；后续 PID 自动归属；外层调度和 Blender 在完整帧后退出；等待后无新批次；管理器重启保留暂停状态；继续保持已有帧哈希，完成后处理并记录退出码；日志和队列重置通过。
- 旧单批任务先完成，再从下一批接管整套脚本，复用原任务 ID；上述暂停、恢复、后处理流程再次通过。
- 浏览器在独立测试服务点击“一键接管”，显示脚本名称、完整 12 帧范围、当前 5–8 批次及日志；按帧暂停后显示 8/12 和“已暂停”，继续按钮可用，无浏览器错误。
- 点击继续后恢复到 12/12，并显示“等待脚本后处理”。Windows 打包程序原生启动、完成记录清理和图片保留通过。

## 3.3.1 验证记录

- 58 项后端/API 测试通过，包括接管中查询真实阶段、重复请求共享进度、失败保留阶段和原因，以及鉴权保护。
- 真实 Blender 接管集成测试通过，完成阶段为第 4 步；原进程继续运行、扫描保留实际范围、按帧暂停与续渲染、完成帧哈希和日志均正常。
- 独立数据目录下通过受控延时演示浏览器提示，验证同时接管两个进程、读取工程阶段说明、用时更新、成功自动收起、错误持续显示和关闭、跨页面保留。无浏览器错误；演示未控制用户进程。
- v3.3.1 Windows EXE 原生启动就绪，完成记录清理和图片保留验证通过。

## 3.3.0 验证记录

只读核对用户问题工程和脚本；所有暂停、继续、删除等操作仅在隔离测试数据和测试进程执行。

- 57 项后端/API 测试通过。新增脚本静态识别覆盖命令行参数、实际目录、帧号模板、固定步长、PNG 格式覆盖、未知路径拒绝、不执行脚本、动态代码拒绝和重复点击合并。
- 实际问题工程只读扫描得到 1200 帧；根据脚本和启动参数自动推导 181–240 帧、60 帧任务、实际 PNG 目录及 ####.png，未修改工程或运行原脚本。
- 真实 Blender 隔离测试：单条 process.adopt 命令接入外部脚本；原 PID 保持运行；扫描显示工程 1200 与任务 10 帧；重新扫描保留脚本子范围；按帧暂停、退出、继续至 10/10，已完成图片哈希不变，日志捕获通过。
- 界面实测：桌面卡片布局、预览加载、任务搜索、无结果提示、状态筛选、筛选时禁止排序、编辑弹窗字段禁用、设置页切换通过。390px 嵌入式手机视口视觉检查通过，使用独立测试服务允许同源预览；正式程序仍禁止被其他页面嵌入。
- 最终 Windows 包浏览器实测：点击一次“一键接管”，自动加入队列，原 PID 继续运行，显示本次 60 帧 / 工程 1200 帧，卡片与详情预览、日志和进度正常。
- Windows EXE 原生 pywebview 启动并就绪，启动清理已完成队列、保留等待任务与图片通过。

以下是历史版本验证记录；历史暗色截图不代表当前界面。

## 3.2.0 验证记录

验证环境与下方 3.1 一致。只使用隔离测试目录与测试进程，没有操作用户实际任务。

- 44 项后端/API 测试通过。新增队列编辑校验、顺序持久化、过期客户端排序拒绝、删除后不重新导入、部分/全部完成重置、归档图片哈希、保留完成帧、运行中等待退出、归档中断后恢复、文件变化保护和启动清理。
- 真实 Blender 验证完成任务重置后再次渲染；旧帧归档、进度回零，源工程哈希不变。重开管理器仅移除完成任务，等待任务、图片和日志保留。
- 真实 Blender 验证运行中请求重置和删除：当前帧保存、进程退出后执行；重置后可以再次启动，删除仍保留完成图片。
- 原有真实 Blender 工程扫描、1200 帧范围、PNG16 RGBA、原路径与文件名、崩溃恢复、预览、定时启动、关闭重开、外部脚本接管和续渲染、缺帧补渲染通过。
- 浏览器手动操作检查：修改名称与帧范围、下移、置顶、重置部分完成任务、删除及刷新后结果保持；重置后旧预览隐藏，删除后列表数量和选中任务更新。截图检查新增按钮与原暗色风格一致。物理鼠标拖动未自动化验证；排序按钮及底层持久化已验证。

- Windows 打包 EXE 的原生 pywebview 窗口就绪检查通过；启动时清理完成队列、保留等待任务与图片通过。

## 3.1.0 历史验证记录

验证环境：Windows 11、Python 3.13.15、Blender 5.1.2、Edge/WebView2。所有测试使用独立临时数据和测试工程，未控制用户正在渲染的任务。

- 31 项后端/API 回归通过：帧事务、完成帧哈希、缺失帧、定时、旧任务导入、认证、Bark、扫描配置与覆盖、60→1200 范围修正、预览完整性、文件事件、限次恢复、密钥轮换、扫描时状态服务响应。
- 真实 Blender 读取 1–1200 帧工程，PNG 16-bit RGBA，保留项目相对路径、关闭扩展名和 50% 分辨率。限定任务渲染 2 帧后，显示任务 2/2 与工程 1200 帧。源 .blend 哈希未变。
- 在测试进程启动后模拟崩溃，Watchdog 等待 5 秒后使用新 PID 自动续渲染，完成帧预览可用，重开管理器任务与完成帧保留。
- 真实 Blender 原有集成测试通过：定时启动、重开管理器后按帧暂停、退出进程、新 PID 继续、完成帧哈希不变、缺帧补渲染；外部脚本接入、首次暂停、协作续渲染、完整日志。
- 浏览器回归通过：多文件导入、原生拖拽接收函数、扫描默认范围、预览、定时、双客户端共享、手机无横向溢出、Watchdog 设置、手动访问密钥使旧设备重新登录、Bark/远程设置，无 JavaScript 错误。物理拖拽未以机器人鼠标操作验证；桌面使用 pywebview 官方 DOM drop/full-path 接口。
- pywebview 原生窗口启动与 UI 就绪检查通过。
- 官方 frpc 0.71.0 的配置 verify、本机 frps 转发验证通过；带工作台密钥访问 200，不带密钥 401，停止后进程退出。未连接公共 frps，实际远程服务需用户填写。

界面截图使用隔离测试任务，预览为测试图像。GPU 计数器可用性仍取决于驱动。后台渲染不承诺固定性能提升。外部任意脚本的内存配置无法自动读取，接管扫描以磁盘工程为准。
- 打包后的 Windows EXE：原生窗口 UI 就绪通过；服务器模式检测到内置 frpc / watchdog，真实扫描 1200 帧工程并渲染指定一帧，JPEG 预览接口通过。
