# 更新记录

## 1.3.1

- 进程扫描先按名称筛选，只读取 Blender 的命令行和资源信息，避免逐个查询所有系统进程。
- 文件检查、定时调度、任务操作与状态采集在独立后台线程串行执行；界面使用状态快照，不等待磁盘操作。
- 日志改为增量读取和追加显示；追赶大量输出时限制单次读取量，界面保留最多 1200 行 / 100000 字符，完整日志仍保存在文件中。
- 仅在内容变化时更新表格行、按钮和进度条，减少滚动和刷新卡顿。
- 外部帧检查仅遍历目录中已有文件，避免对尚未生成的每一帧重复查询磁盘。

## 1.3.0

- 任务和本机进程页增加完成帧数、百分比、本次运行时长、CPU 与内存占用。
- 本机进程自动刷新改为约每 2 秒，未知渲染进度明确标注待接入识别。
- 支持接入单个 Python 脚本逐帧输出 PNG 的已有后台进程，确认输出与范围后统一管理。
- 首次暂停观察新 PNG 完整保存后结束原进程；续渲染后由桥接脚本支持帧边界协作退出。
- 保留原脚本与参数；首次接入记录保存事件，重启后记录完整 stdout。
- 底部署名按钮打开 B站个人主页。

## 1.2.0

- 使用自绘深色圆角滚动条，按内容溢出情况显示或隐藏。
- 修复浅色进度条边框和页面容器外框。
- 保持水平、垂直滚动条拖动与内容位置同步。
- 加入底部署名：B站：@in_uni。

## 1.1.0

- 深色侧栏、状态卡片、统一的圆角按钮与表单。
- 任务进度和日志集中展示，日志支持事件分类着色。

## 1.0.0

- 本机 Blender 进程发现。
- 后台逐帧渲染、完成当前帧后暂停并退出、按保存进度继续。
- 定时开始和暂停，任务与日志持久保存。
