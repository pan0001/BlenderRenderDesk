# 2.0.0 验证记录

日期：2026-09-22。Windows 11、Python 3.13.15、PySide6 6.11.2、Blender 5.1.2。

## 单元测试

`python -m unittest -v test_engine.py`：16 项通过。

覆盖：SQLite 重开、1.x 导入、不改写旧配置、错误迁移保留原文件、独立输出目录、帧范围校验、工程修改拒绝续渲染、过期计划取消、计划只执行一次、手动暂停取消计划、PNG CRC/截断识别、同大小内容篡改检测、缺失输出识别、UTF-8 增量日志、日志读取上限、PID 重用防护。

## 真实 Blender 测试

`python test_blender_integration.py --blender <Blender 路径>`：通过。

- 定时启动 4 帧 Cycles CPU 渲染，使用中文和空格的 .blend 路径。
- 渲染中关闭并重新创建控制器，重新识别原 PID。
- 请求暂停，确认至少一帧完整保存，Blender 进程退出。
- 用新 PID 继续到 4 帧，已有图片 SHA256 不变，累计运行时间增加。
- 删除测试任务最后一张图，继续后仅补回缺失帧。
- 启动一个独立的 6 帧渲染脚本，接入它的进程。
- 首次暂停检测完整 PNG 后停止旧进程；随后通过协作执行器继续并再次按帧暂停。
- 最终 6 帧全部完成，先前完整帧 SHA256 不变，原脚本 stdout 出现在日志中。

仅操作测试脚本自行创建的任务和进程，未控制用户原有渲染任务。

## 界面和响应

`python test_desktop.py`：通过。

- 空列表、35 行任务、窄窗口、添加任务表单和自定义滚动条。
- 按运行状态启用/禁用开始与暂停，识别已管理 PID，限制日志行数。
- 在服务层每次快照故意延迟 800 ms 时，10 ms UI 心跳实测最大间隔约 17 ms；正常关闭服务线程。
- 检查了实际渲染的截图；无旧式 Windows 亮色滚动条。

该数字是本机受控测试结果，不是所有机器上的性能保证。GPU 计数器在本机能够初始化；未逐一验证不同厂商显卡及驱动，缺失采样会显示“—”。

## Windows 便携包

最终 PyInstaller EXE 已通过独立启动、服务快照、SQLite 初始化和正常关闭线程的自检。构建脚本优先使用 Windows 系统 DLL 搜索路径，避免从无关开发工具中收集同名但 ABI 不兼容的 ICU 库。

## 功能边界

任意进程的历史 stdout 无法补接。首次外部接入只支持单 Python 脚本、同步逐帧 PNG 后台渲染。外部脚本的初始化副作用会在续渲染时重新执行。暂停释放资源需等待当前帧完成。多视图、直接视频输出、模拟状态恢复和 File Output 节点事务未实现。

未复制参考软件的源码或资产到发布目录；代码与来源说明见 PROVENANCE.md，依赖许可见 THIRD_PARTY_NOTICES.md。
